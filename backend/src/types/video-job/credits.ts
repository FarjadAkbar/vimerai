/** Credits charged per Video Job (including Regenerate as a new job). */
export const VIDEO_JOB_CREDIT_COST = 2;

/** Target duration for MVP Video Jobs (~15–30s short). */
export const VIDEO_JOB_DURATION_TARGET_SECONDS = 20;
