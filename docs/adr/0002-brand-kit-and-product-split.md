# Brand Kit and Product are separate entities

Generation context is modeled as a Brand Kit plus a Product, not a combined Product Kit. Phase 1 uses minimum required fields on each; remaining brand/product fields stay optional. The filesystem Product Kit is deprecated so brand voice can stay reusable across products and UI accents can follow the Brand Kit.
