# Phase 1 has no Workspace — resources are user-owned

Brand Kits, Products, and Generations belong directly to the User. Workspace, teams, and invites are deferred. A premature empty Workspace wrapper was rejected as schema theater until real multi-seat demand exists.
