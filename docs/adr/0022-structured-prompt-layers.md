# Generation prompts use fixed structured layers

Text generation assembles context in layers: quality/safety rules, Brand Kit, Product, Goal/Length Tier/platforms, then an output schema for the Creative Brief or target Content Output section. Ad hoc prompt strings and embedding-based brand memory were rejected for Phase 1 — layers stay testable, consistent, and cheap to reuse on section regenerate.
