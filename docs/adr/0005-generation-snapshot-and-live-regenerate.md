# Generations snapshot context; regenerate uses live Brand Kit and Product

A Generation stores the Brand Kit and Product fields used at create time so past Content Outputs stay faithful to what was produced. Regenerate and section-regenerate pull the current Brand Kit and Product. Chosen over live-only references (which rewrite history) and snapshot-only regenerate (which fights brand updates).
