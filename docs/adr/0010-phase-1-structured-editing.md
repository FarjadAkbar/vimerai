# Phase 1 editing is structured fields plus optional section regenerate

Users edit Social Posts and Reel Storyboards via structured fields and scene lists (including reorder). Small tweaks are manual edits saved with no AI call (saves tokens). Section regenerate is optional when the user wants the AI to rewrite a chosen part. Freeform canvas editing and version history are deferred.

