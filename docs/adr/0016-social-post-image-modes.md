# Social Post image mode: Product photo default, AI image optional

A Phase 1 Social Post includes a caption package plus a Post image. Users can use a Product photo (default when Product images exist) or optionally generate an AI still conditioned on Product images. Always-on AI images and caption-only posts were rejected — ecommerce trust favors real product photos, with AI as an opt-in creative path.
