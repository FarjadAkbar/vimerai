# Generation billing is weighted by Length Tier

A Generation consumes weighted credits: Teaser costs less than Promo (e.g. 1 vs 4). Flat per-Generation pricing and separate video/copy meters were rejected — flat invites always-Promo abuse; split meters complicate a single Generate action.
