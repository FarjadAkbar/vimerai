# Generate happy path is Product + Goal; the rest are defaults

Phase 1 Generate requires Product and Goal. Defaults are Teaser, Instagram feed, Instagram Reels, and Product photo for Post image; Brand Kit auto-selects when uniquely linked. Length Tier, platforms, image mode, and multi-Brand Kit choice sit in Options. Preserves a two-step create flow despite a rich Generation bundle.
