# Prompt Studio is removed from the Phase 1 product path

User-facing prompt template CRUD (Prompt Studio) is retired for Phase 1. Generation prompts come from system templates plus Brand Kit, Product, Goal, and optional Brand Kit AI instructions. Keeping a second consumer prompt system was rejected because it fights brand consistency and duplicates control surfaces.
