# Generation picks Brand Kit only when a Product has multiple links

At Generation time the user selects Product and Goal. If the Product is linked to exactly one Brand Kit, that Brand Kit is used automatically. If linked to multiple, the user must choose which Brand Kit voice applies. Keeps the common path at Product → Goal → Generate without blending brand voices.
